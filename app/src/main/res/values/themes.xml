<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Bankai" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary Color-->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/grey</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/light_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="autoCompleteTextViewStyle">@style/CustomSearchView</item>

    </style>
    <style name="CustomSearchView" parent="Widget.AppCompat.Light.AutoCompleteTextView">
        <item name="fontFamily">@font/comfortaa_regular</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="CustomSpinner">
        <item name="fontFamily">@font/comfortaa_regular</item>
    </style>

    <style name="FiltersThemedButton">
        <item name="android:layout_marginTop">6dp</item>
        <item name="toggle_backgroundColor">@color/white</item>
        <item name="toggle_borderColor">@color/green</item>
        <item name="toggle_borderWidth">1dp</item>
        <item name="toggle_fontFamily">/fonts/comfortaa_light.ttf</item>
        <item name="toggle_selectedBackgroundColor">@color/opacity_green</item>
        <item name="toggle_selectedBorderColor">@color/green</item>
        <item name="toggle_selectedTextColor">@color/green</item>
        <item name="toggle_textColor">@color/green</item>
        <item name="toggle_textSize">10sp</item>
        <item name="toggle_selectedBorderWidth">1dp</item>
    </style>

</resources>